name: router
description: A turbo router brick.
version: 0.1.0+1
environment:
  mason: ^0.1.0
vars:
  featureName:
    type: string
    description: The name of the feature (e.g. auth -> lib/auth/routers/).
