name: view
description: A turbo view brick.
version: 0.1.0+1
environment:
  mason: ^0.1.0
vars:
  featureName:
    type: string
    description: The name of the feature (e.g. auth -> lib/auth/views/).
  viewName:
    type: string
    description: The name of the view (e.g. Turbo -> TurboView).
