name: document-service
description: A turbo document-service brick.
version: 0.1.0+1
environment:
  mason: ^0.1.0
vars:
  collectionObjectName:
    type: string
    description: Name of the DTO without 'Dto' (user -> UserDto) (singular).
